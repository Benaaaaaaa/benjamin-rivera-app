import React from 'react';
import ReactDOM from 'react-dom/client';
import { AutomoApp } from './AutomoApp';

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <AutomoApp />
    </React.StrictMode>
)